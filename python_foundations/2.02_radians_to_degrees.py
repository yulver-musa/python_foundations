# Deposit calculator:
# Formula: sum = deposit + months * ((deposit * annual_interest) / 12).
# Input: deposit, months, annual interest %.
# Output: final amount.

from math import pi
radians = float(input())

degrees = radians * 180 / pi

print(degrees)
